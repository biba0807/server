meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: java -jar spigot-1.20.1.jar -nogui
  args: null
  persistenceMount: /data
  containerPort: 25565,27017
